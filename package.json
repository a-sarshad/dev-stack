{
  "name": "dev-agents",
  "version": "0.0.1",
  "private": true,
  "description": "Collection of dev agents for code quality, consistency, and fixing",
  "packageManager": "pnpm@11.1.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
