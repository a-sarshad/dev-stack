# REPLACE-ME — RTL Rules

<!--
اینجا چی می‌ره: قوانین RTL که خاص API/رفتار این DS ـه — مثلاً این کتابخانه
logical props رو چطور پیاده می‌کنه، کدوم کامپوننت‌هاش به `dir` نیاز دارن،
DOM order برای کدوم پترن‌ها (icon/switch/dialog-close) باید چی باشه.
این‌ها قرارداد کتابخانه‌ان، برای هر پروژه‌ای که این DS رو با RTL استفاده کنه
یکسانن.

اینجا چی نمی‌ره: اینکه یک پروژه‌ی خاص RTL-only است یا دوزبانه، یا locale/فونت
پروژه. اون تنظیمات پروژه‌ست (`.dev-engine.json` یا project-context)، نه
قانون DS. اگه این DS اصلاً جهت‌آگاه نیست (مثلاً یک CSS framework بدون هیچ
RTL primitive)، همین رو صریح بنویس — خالی نذار.
-->

## Direction Setup

`REPLACE-ME`

## DOM Order / Logical Props

`REPLACE-ME`
