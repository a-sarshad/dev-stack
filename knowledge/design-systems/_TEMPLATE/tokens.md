# REPLACE-ME — Token Reference

<!--
اینجا چی می‌ره: اسم‌های semantic token این DS و نقش‌شون (مثلاً `bg.subtle` یعنی چی،
`space.4` چند پیکسله) — چیزی که کتابخانه به عنوان API تعریف کرده.

اینجا چی نمی‌ره: مقدار واقعی رنگ brand هر پروژه (hex کد، رنگ اصلی، فونت پروژه).
اون VALUE ـه، نه NAME — جاش `{project}/.claude/context/` یا theme config خودِ
پروژه‌ست. تست سریع: اگه تغییرش فقط ظاهر رو عوض می‌کنه (رنگ/فونت) → پروژه‌ای.
اگه تغییرش یعنی یه token اسمش/معنیش عوض شده → DS-level، همین‌جا.
-->

| Token | معنی/مقدار پیش‌فرض DS |
|-------|------------------------|
| `REPLACE-ME` | `REPLACE-ME` |
